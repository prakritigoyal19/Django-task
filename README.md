# Django-task
Django task for IE website team
-----------------------------------------------------------------------------------------------------------------------------
To run:
*Clone the program.
*Install Django.
*run the command "python manage.py runserver"
*change to /new url
